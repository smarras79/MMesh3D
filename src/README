The functions that belong to "myCFD_library", must be linked to this
directory in order to be compiled. 
Furthermore, remember to include them into each code that uses them:

e.g.:
#include "$HOME/my/path/to/myCFD_library/PRINT.h"


Write me if you need help
smarras@njit.edu

